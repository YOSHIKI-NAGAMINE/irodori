<div class="wrapper">
  <div class="side-bar">

    <div class="side-bar-header">
      <div class="siori-title">
        <%= @siori.title %>
      </div>
      <div>
        <%= "期間：#{@siori.start_day}〜【#{@siori.day}日間】 参加者：#{User.find(@siori.user_ids[0]).nickname}/ #{User.find(@siori.user_ids[1]).nickname}/ #{User.find(@siori.user_ids[2]).nickname}" %>
      </div>
    </div>

    <% n=1 %>
    <% @siori.day.to_i.times do %>
    <div class="schedule">
      <div class="day-schedule">
        <div class="schedule-day">
        <%= "Day#{n}" %><%= link_to("<予定を追加する>", "/sioris/#{@siori.id}/schedules/new", {class: "add-schedule"}) %>
        </div>
        <div class="col-md-6">
          <div class="row g-0 border rounded overflow-hidden flex-md-row mb-4 shadow-sm h-md-250 position-relative">
            <div class="col p-4 d-flex flex-column position-static">
              <strong class="d-inline-block mb-2 text-primary">AM9:00</strong>
              <h3 class="mb-0">ホテルで朝食</h3>
              <div class="mb-1 text-muted">場所：Hotel's restaulant＜GoogleMapLink＞</div>
              <ul class="schedule-memo">
                <li>朝食券を持って行くのを忘れずに！</li>
                <li>会計時にクーポンを見せれば割引！</li>
                <li>クロワッサンが美味しいらしい！</li>
              </ul>
              <a href="#" class="stretched-link">編集</a>
            </div>
            <div class="col-auto d-none d-lg-block">
              <%= image_tag 'omrice.png', size: '200x200'%>
            </div>
            <div class="col-auto d-none d-lg-block">
              Album→
              <%= image_tag 'icon_album.png', size: '40x40'%>
            </div>
          </div>
        </div>
      </div>
    </div>
    <% n += 1%>
    <% end %>

  </div>
  <div class="chat">
    <%= render "main_chat" %>
  </div>
</div>